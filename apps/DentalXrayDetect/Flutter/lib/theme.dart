import 'package:flutter/material.dart';

/// OraLens visual theme — a clinical dark palette (radiograph viewer feel).
class AppTheme {
  static const Color bg = Color(0xFF0B141B);
  static const Color surface = Color(0xFF101C26);
  static const Color accent = Color(0xFF2FD4C4); // teal — imaging/diagnostic
  static const Color accentSoft = Color(0xFF1C3A40);
  static const Color textPrimary = Color(0xFFEAF2F5);
  static const Color textMuted = Color(0xFF8399A3);
  static const Color warn = Color(0xFFFFB020);
}
