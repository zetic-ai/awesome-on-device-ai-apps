// Template for lib/config/secrets.dart (which is GITIGNORED — never commit the real key).
//
// Copy this file to `secrets.dart` in the same directory and replace the
// placeholder with your real ZETIC Melange personal key:
//
//   cp lib/config/secrets.example.dart lib/config/secrets.dart
//
// The real `secrets.dart` is listed in .gitignore and must never be tracked,
// pasted into HANDOFF.md, or committed to any doc.
const String zeticPersonalKey = '<ZETIC_PERSONAL_KEY>';
