<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Microphone capture for on-device voice analysis. Audio never leaves the phone. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Front-camera capture for on-device facial-expression analysis. Frames never leave the phone. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Only used to download + cache the Melange models on first launch. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Light haptics on button taps / when the report is ready (normal permission). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aiberry">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Aiberry">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
