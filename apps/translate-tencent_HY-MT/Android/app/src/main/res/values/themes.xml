<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark, action-bar-free shell. The launch window background is black so it matches the
         programmatic Compose UI (mirrors the iOS app's forced dark appearance). -->
    <style name="Theme.OfflineTranslator" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
